import queryParams from 'xdam-saas-fe/utils/query-params';
import { module, skip } from 'qunit';

module('Unit | Utility | query-params', function(hooks) {

  // Replace this with your real tests.
  skip('it works', function(assert) {
    let result = queryParams();
    assert.ok(result);
  });
});
